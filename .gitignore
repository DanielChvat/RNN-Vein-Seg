__pycache__
.DS_Store
*.zip

# Pipeline data. Generated by `python -m veinseg prepare`.
data/raw/
data/processed/
data/filtered/

# Pre-reorganisation locations. Kept so that a machine still holding the old
# layout (the Zeiss PC) cannot accidentally commit gigabytes of frames.
raw_data/
processed_data/
filtered_data/

checkpoints/

# Tracked from before this ignore was added; 2760 files stay in the index.
filtered_data_augmented/

# figures/vis_outputs/ is deliberately NOT ignored -- 848 PNGs are tracked on
# purpose.
