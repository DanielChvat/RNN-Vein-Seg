# Python 3.10+
#
# These are minimum versions, not a `pip freeze` of a verified environment --
# unlike the pinned lists you may be used to. Nothing here has been resolved or
# installed on the machine this file was written on, so treat it as the
# dependency set, not a reproducible lock. Regenerate with `pip freeze` from a
# working install when you want one.
#
# Install with pip, not conda:  pip install -r requirements.txt
# Or install the package itself: pip install -e .

torch>=2.1
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7
albumentations>=2.0.8
pillow>=10.0
tqdm>=4.65
PyYAML>=6.0
